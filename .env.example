# LINE Messaging API (from LINE Developers Console)
LINE_CHANNEL_SECRET=your_channel_secret_here
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here

# Nextcloud WebDAV (URL = server only; path /remote.php/dav/files/USER/ is added by app)
NEXTCLOUD_URL=https://your-nextcloud.example.com
NEXTCLOUD_USER=your_username
NEXTCLOUD_PASSWORD=your_app_password
# Folder name under your files (LINE_Backup or /LINE_Backup/ both ok)
NEXTCLOUD_BASE_PATH=LINE_Backup
# Reply/push messages (true=on, false=off). Off = no "收到檔案..." / "✅ 已備份", saves LINE send quota; turn on when debugging.
ENABLE_LINE_REPLIES=false
# Source folders: send "1" then media -> save under Amigo, "2" -> Ben, etc.; no number or "0"/"other" -> other
SOURCE_MAP=1:Amigo,2:Ben,3:Mom
# Optional: file to store source map. If set and file exists, overrides SOURCE_MAP. Editable in browser at /admin.
SOURCE_MAP_FILE=data/source_map.json
# Optional: persist source to file (path relative to cwd). Empty = disabled. Restart keeps last number.
SOURCE_STATE_FILE=data/source_state.json
# Optional: password for /admin (source mapping UI). Empty = no auth (only use on trusted network).
# ADMIN_PASSWORD=your_admin_password
# Optional: GitHub repo URL shown in home page footer (e.g. https://github.com/user/line-backup-bot).
# GITHUB_REPO=https://github.com/yourusername/line-backup-bot
# Run container as this user so ./data is writable. On the server run: id -u  and  id -g  then set:
# UID=1000
# GID=1000
# Optional: max file size in MB (0 = no limit). Larger files are skipped.
MAX_FILE_SIZE_MB=0
# Optional: backup plain text (no URL) to daily notes.txt in Nextcloud. true/false.
# ENABLE_TEXT_BACKUP=false
# Optional: file to store uploaded hashes for duplicate skip. Empty = disabled.
# UPLOADED_HASHES_FILE=data/uploaded_hashes.json

ADMIN_PASSWORD=your_admin_password
